# college-advisor-rebuild

## Professor research

The `search_rate_my_professor` and `search_reddit` runtime tools use Tavily
domain-restricted search. Set `TAVILY_API_KEY` in the local, uncommitted `.env`
file before running live professor searches. Reddit OAuth credentials and a
Rate My Professors API key are not used.
